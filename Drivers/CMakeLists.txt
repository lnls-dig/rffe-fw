set(DRIVERS_PATH ${CMAKE_CURRENT_SOURCE_DIR})

set(DRIVERS_SRCS
  ${DRIVERS_PATH}/Drivers.cpp
  )

set(DRIVERS_HDRS
  ${DRIVERS_PATH}/
  )

set(PROJ_SRCS ${PROJ_SRCS} ${DRIVERS_SRCS} PARENT_SCOPE)
set(PROJ_HDRS ${PROJ_HDRS} ${DRIVERS_HDRS} PARENT_SCOPE)
