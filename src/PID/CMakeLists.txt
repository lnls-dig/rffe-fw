set(PID_PATH ${CMAKE_CURRENT_SOURCE_DIR})

set(PID_SRCS
  ${PID_PATH}/PID.cpp
  )

set(PID_HDRS
  ${PID_PATH}/
  )

set(PROJ_SRCS ${PROJ_SRCS} ${PID_SRCS} PARENT_SCOPE)
set(PROJ_HDRS ${PROJ_HDRS} ${PID_HDRS} PARENT_SCOPE)
